import Note from './visualizer';

document.addEventListener('DOMContentLoaded', () => {
  let canvas = document.getElementById('content');
  let ctx = canvas.getContext('2d');

  let note = window.Note;
  
});
